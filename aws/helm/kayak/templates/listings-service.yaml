{{- if .Values.listingsService.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.listingsService.name }}
  namespace: {{ .Values.global.project }}-{{ .Values.global.environment }}
  labels:
    {{- include "kayak.labels" . | nindent 4 }}
    app.kubernetes.io/component: listings
spec:
  replicas: {{ .Values.listingsService.replicaCount }}
  selector:
    matchLabels:
      {{- include "kayak.selectorLabels" . | nindent 6 }}
      app.kubernetes.io/component: listings
  template:
    metadata:
      labels:
        {{- include "kayak.selectorLabels" . | nindent 8 }}
        app.kubernetes.io/component: listings
    spec:
      containers:
      - name: {{ .Values.listingsService.name }}
        image: {{ include "kayak.image" (dict "root" . "image" .Values.listingsService.image) }}
        imagePullPolicy: {{ .Values.global.imagePullPolicy }}
        ports:
        - name: http
          containerPort: {{ .Values.listingsService.service.targetPort }}
          protocol: TCP
        env:
        {{- range .Values.listingsService.env }}
        - name: {{ .name }}
          value: {{ .value | quote }}
        {{- end }}
        envFrom:
        {{- range .Values.listingsService.envFrom }}
        - {{ . | toYaml | nindent 10 | trim }}
        {{- end }}
        resources:
          {{- toYaml .Values.listingsService.resources | nindent 10 }}
        livenessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/live
            port: http
          initialDelaySeconds: 10
          periodSeconds: 5
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.listingsService.name }}
  namespace: {{ .Values.global.project }}-{{ .Values.global.environment }}
  labels:
    {{- include "kayak.labels" . | nindent 4 }}
    app.kubernetes.io/component: listings
spec:
  type: {{ .Values.listingsService.service.type }}
  ports:
  - port: {{ .Values.listingsService.service.port }}
    targetPort: {{ .Values.listingsService.service.targetPort }}
    protocol: TCP
    name: http
  selector:
    {{- include "kayak.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: listings
{{- end }}
